# Postgres na Neon (ou outro Postgres). Copia para .env.local em desenvolvimento.
# Na Vercel: Settings → Environment Variables → DATABASE_URL
#
# Cria uma base grátis em https://neon.tech e cola a connection string (PostgreSQL).
DATABASE_URL=
